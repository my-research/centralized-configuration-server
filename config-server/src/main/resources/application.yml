server:
  port: 8888


spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/my-research/centralized-configuration
          # default-label: main # master ???? default ?
